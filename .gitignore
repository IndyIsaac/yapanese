node_modules/
output/
.playwright-cli/

# Generated dev preview of the renderer with a stubbed API (tools/make-preview.js)
src/renderer/preview.html

# Design-process working files (internal)
.impeccable/

# App assets are generated by tools/make-icon.ps1, but committed so a clone
# has an icon without running PowerShell first.

# Internal product notes: describes the author's machine and habits,
# not something a public clone needs.
PRODUCT.md

# electron-builder output
dist/
